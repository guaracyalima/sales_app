'use strict';

import React from 'react';

import {
  ,
} from 'react-native';

const SubHeader = (props) => {
  render() {
    return (
      <View />
    );
  }
}

const styles = StyleSheet.create({

});

export default SubHeader;