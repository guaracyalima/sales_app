'use strict';

import React from 'react';

import {
  View
} from 'react-native';

const Buttons = (props) => {
  render() {
    return (
      <View />
    );
  }
}

const styles = StyleSheet.create({

});

export default Buttons;